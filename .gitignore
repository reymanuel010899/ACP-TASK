.venv/
__pycache__/
*.pyc
.pytest_cache/

# Claves privadas ed25519 de Principals — nunca versionadas (KTD8)
agents/**/keys/
web/keys/
*.key

# Settings locales de Claude Code (por usuario)
.claude/settings.local.json

# Node
node_modules/

# Coverage
.coverage

# Claude Code temp agent worktrees (nunca versionar)
.claude/worktrees/
.venv_test/
dump.rdb
.env
.env.*

# Local runtime databases and SQLite sidecars
tessera-*.db
tessera-*.db-shm
tessera-*.db-wal

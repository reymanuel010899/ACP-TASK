"""AgentTrust provider agent (unit U6).

Independently buildable from the published spec (RFC-0001/RFC-0002 and the
JSON schemas): this package MUST NOT import from ``services`` or
``registry`` — all interaction with them happens over HTTP.
"""
